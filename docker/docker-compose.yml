version: "3.8"

services:

  ml-service:
    build: ../ml-service
    container_name: movie-ml
    ports:
      - "8000:8000"

  backend:
    build: ../backend
    container_name: movie-backend
    ports:
      - "5000:5000"
    depends_on:
      - ml-service